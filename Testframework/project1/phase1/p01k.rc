/* Check 1 (p01k.rc) */
// Expected: Compile: failure.
// Incompatible type bool to binary operator %, equivalent to int expected.

int i;
bool b;

function : int main() {
    i = i % b;
    return 0;
}
